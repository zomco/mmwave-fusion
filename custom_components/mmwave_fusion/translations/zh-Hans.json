{
  "config": {
    "step": {
      "user": {
        "title": "MMWave 融合",
        "description": "启用多雷达融合后端。雷达、区域和摄像头都在 MMWave 卡片里配置，这里无需填写任何内容。"
      }
    },
    "abort": {
      "already_configured": "MMWave 融合已配置。"
    }
  },
  "issues": {
    "radar_unavailable": {
      "title": "雷达 {radar_id} 没有上报数据",
      "description": "融合系统 `{fusion_id}` 读取雷达 `{radar_id}` 的实体当前不可用，该雷达没有任何贡献：它的目标不会出现在融合视图中，仅由它覆盖的区域会一直显示为空。\n\n常见原因：设备掉线、断电，或 ESPHome API 不可达。请先在 Home Assistant 中查看该设备页面，再检查供电与接线。\n\n雷达恢复上报后此提示会自动消失。"
    },
    "radar_stale": {
      "title": "雷达 {radar_id} 停止发送数据帧",
      "description": "融合系统 `{fusion_id}` 中的雷达 `{radar_id}` 仍然在线，但其目标帧已超过配置的超时时间没有更新。也就是说设备本身可达，而它后面的雷达模块不再出数据。\n\n这通常指向串口而非网络：接插件松动、UART 引脚虚焊，或雷达模块死机。重启 ESP 不会给雷达模块断电，因此通常需要整体断电重上。\n\n数据帧恢复后此提示会自动消失。"
    },
    "radar_calibration": {
      "title": "雷达 {radar_id} 可能标定有误",
      "description": "在至少 100 次观测中，雷达 `{radar_id}` 上报的目标只有不到五分之一落在融合系统 `{fusion_id}` 配置的房间范围内。\n\n这种情况说明雷达能看到人，但坐标变换把人放到了房间之外——通常是安装位置偏移填错，或偏航角差了 90 度或 180 度。请在 mmWave 卡片的校准页重新标定。\n\n目标重新落回房间内后此提示会自动消失。"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "历史保留期",
        "description": "融合数据库保留记录的时长。位置点占据了文件的绝大部分——默认每条轨迹每半秒存一个点，在开发测试台上约合每天 50 MB。决定这个频率的是轨迹质量选项 `persist_interval_s`，不是融合频率：调高融合频率只会让跟踪更平滑，并不会多存数据。事件和轨迹属于元数据，开销极小，因此通常保留更久。\n\n缩短窗口后，多余的数据会在下一轮清理时删除。SQLite 会复用释放出的页，但不会自动缩小文件；调用 `mmwave_fusion.vacuum_database` 服务可把空间还给磁盘。",
        "data": {
          "point_retention_days": "位置点保留天数",
          "event_retention_days": "事件与轨迹保留天数"
        },
        "data_description": {
          "point_retention_days": "占用热力图最多只能回看这么久，其最宽视图为 7 天。",
          "event_retention_days": "不得短于位置点保留天数，否则位置点会比它所属的轨迹活得更久。已保存录像的事件永不删除。"
        }
      }
    },
    "error": {
      "events_shorter_than_points": "事件保留时长不得短于位置点。否则位置点所属的轨迹会先被删除，而位置点仍然留在库里——既占空间，又不再出现在任何界面上。"
    }
  }
}
